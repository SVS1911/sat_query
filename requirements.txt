# Core (required to run the base app out of the box)
numpy>=1.24
pillow>=10.0
gradio>=4.0
tifffile>=2023.7.10

# Optional: geo-aware TIFF reading with CRS/geotransform support
# (falls back to tifffile/PIL automatically if not installed)
rasterio>=1.3

# Required only for the real fine-tuning path (training/download_bigearthnet.py +
# training/finetune_bigearthnet.py) — NOT needed to run the base app.
# gdown>=5.0        # fetches BigEarthNet-mini/medium/full from Google Drive
# hub>=2.0          # Activeloop hub format used by the pre-packaged dataset
# torch>=2.1        # trains the land-cover CNN and is needed to load its checkpoint

# Optional Qwen3-VL adapter runtime (install only on a suitable GPU machine).
# transformers>=4.57
# peft>=0.17
# accelerate>=1.7
# qwen-vl-utils>=0.0.14
